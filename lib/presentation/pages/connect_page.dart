// lib/presentation/pages/connect_page.dart

import 'package:flutter/material.dart';

class ConnectPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Connect'),
      ),
      body: Center(
        child: Text('Tela de Connect'),
      ),
    );
  }
}